#include <iostream>


//VS Code:
  //to build: Cntrl+Shift+B
  //to run: Cntrl+F5
  //change  "program": "${workspaceFolder}/hello" in launch.json
  // import c/c++ code profile under .vscode folder to setup VS Code editor with required plugins and configurations
  //and select it as the profile for the current workspace.

//VIM: Press F8 ro compile-run
//Make sure .vimrc in PWD


using namespace std;

int main() {

    cout << "Hello cpp!\n";
    return 0;
}